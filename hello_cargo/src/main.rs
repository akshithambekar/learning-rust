fn main() {
    println!("Hello, world!");
}

// cargo build - build executable, follow with ./target/debug/<exec_name>
// cargo run - rebuild if needed, then run executable
// cargo check - check for errors without building

// cargo build --release builds the executable in /target/release instead
