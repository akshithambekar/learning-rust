fn main() { // always the first thing that runs in an executable Rust program
    println!("Hello, world!");
    // macros let you write code to generate code to extend Rust syntax
    // ! = macro. no ! = function
}
